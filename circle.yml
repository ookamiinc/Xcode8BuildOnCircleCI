machine:
  xcode:
    version: "8.0"
  timezone:  Asia/Tokyo
  environment:
    LANG: en_US.UTF-8
test:
  override:
    - env | grep -v MATCH_PASSWORD
    - bundle exec env | grep -v MATCH_PASSWORD
    - bundle exec fastlane adhoc
